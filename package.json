{
  "name": "allure-storefront",
  "version": "1.0.0",
  "description": "ALLURE perfume storefront — static frontend + Vercel serverless API (Postgres); optional local Express server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "express": "^4.19.2",
    "multer": "^2.0.1"
  }
}
